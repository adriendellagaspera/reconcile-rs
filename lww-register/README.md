# lww-register

Last-write-wins domain types used by reconcile: entries, timestamps, clock and persistence contracts,
and key/value bounds.

This package is an implementation detail. Applications should depend on reconcile and use its
re-exported API.

Licensed under MIT OR Apache-2.0.
