# reconcile-gossip

Network adapter crate used by reconcile: datagram transport, wire encoding, authentication, replay
protection, and peer discovery.

This package is an implementation detail. Applications should depend on reconcile and use its
re-exported API.

Package name: reconcile-gossip. Crate name inside the workspace: gossip.

Licensed under MIT OR Apache-2.0.
