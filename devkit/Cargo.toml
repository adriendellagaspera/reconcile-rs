[package]
name = "devkit"
version = "0.0.0"
edition = "2021"
rust-version = "1.85"
license = "MIT OR Apache-2.0"
description = "Internal bench/measurement infrastructure shared across this workspace's own benches. Never published."
publish = false

[dependencies]
rbsr = { version = "0.2.1", path = "../rbsr" }
rsos = { version = "0.5.0", path = "../rsos" }
bincode = "1.3.3"
rand = "0.8.5"
